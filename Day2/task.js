var module = module.exports = function ()
{
    // multiplication table of enter number
    for (let i = 1; i <= 10; i++)
    {
      let j=3;
     console.log(`  ${j} * ${i} = ${j*i} `) ;
    }
}